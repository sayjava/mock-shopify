{
    "cart": {{> cart num_of_lines=(select num_of_lines 5)}},
    "userErrors": [
        {{#if error}}
            {
                "message": "{{error.message}}",
                "field": ["{{error.field}}"],
                "code": "{{error.code}}"
            }
        {{/if}}
    ]
}