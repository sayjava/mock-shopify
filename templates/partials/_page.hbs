{
    "body": {{{nlbr (faker 'lorem.lines' min=30 max=50)}}},
    "bodySummary": {{{nlbr (faker 'lorem.lines' min=10 max=20)}}},
    "createdAt": "{{faker 'date.past'}}",
    "handle": "{{faker 'string.uuid'}}",
    "id": "{{faker 'string.uuid'}}",
    "metatfield": {
        "key": "{{faker 'lorem.word'}}",
        "namespace": "{{faker 'lorem.word'}}"
    },
    "metafields": [
        {{#repeat 3}}
            {
                "key": "{{faker 'lorem.word'}}",
                "namespace": "{{faker 'lorem.word'}}"
            }
        {{/repeat}}
    ],
    "onlineStoreUrl": "{{faker 'internet.url'}}",
    "seo": {
        "title": "A mock Shopify page using a mock shopify server",
        "description": "A mock Shopify page using a mock shopify server"
    },
    "title": "A mock Shopify page using a mock shopify server",
    "updatedAt": "{{faker 'date.past'}}"
}