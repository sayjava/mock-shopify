{
    "amount": "{{faker "number.float" min=(select min 50) max=(select max 100) precision=0.01}}",
    "currencyCode": "{{get path=(select context.variables.country "US") context=context.currency}}"
}